defaults:
  - base_paths

dataset_root: ${data_root}/Replica
dataset_config: ${repo_root}/dataset/dataconfigs/replica/replica.yaml
scene_id: room0
render_camera_path: ${repo_root}/dataset/dataconfigs/replica/replica_room0.json